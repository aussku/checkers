services:
  - type: web
    name: three-player-checkers
    env: node
    plan: free
    buildCommand: npm --prefix backend ci
    startCommand: npm --prefix backend start
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
